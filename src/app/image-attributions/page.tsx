import { ImageAttributionsPageContent } from "@/components/site/ImageAttributionsPageContent";

export const metadata = {
  title: "Image attributions",
};

export default function ImageAttributionsPage() {
  return <ImageAttributionsPageContent locale="en" />;
}
