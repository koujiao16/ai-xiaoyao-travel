import { ImageAttributionsPageContent } from "@/components/site/ImageAttributionsPageContent";

export const metadata = {
  title: "图片来源",
};

export default function ZhImageAttributionsPage() {
  return <ImageAttributionsPageContent locale="zh" />;
}
